services:
  - type: pserv
    name: sao-mysql
    plan: free
    runtime: docker
    autoDeploy: false
    envVars:
      - fromGroup: mysql

  - type: web
    name: sao-web
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile.prod
    envVars:
      - key: SAO_PROFILE
        value: prod
      - fromGroup: mysql

envVarGroups:
  - name: mysql
    envVars:
      - key: MYSQL_DATABASE
        value: sao_db
      - key: MYSQL_USER
        value: saoadmin
      - key: MYSQL_PASSWORD
        value: saoadmin
      - key: MYSQL_ROOT_PASSWORD
        generateValue: true
      - key: DB_HOST
        value: sao-mysql # ← ホスト名をサービス名に
